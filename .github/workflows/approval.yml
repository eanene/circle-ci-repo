name: Approval Workflow

on:
  push:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Running Step 1"
      - name: Step 2
        run: echo "Running Step 2"

  job2:
    runs-on: ubuntu-latest
    needs: job1
    # if: github.event_name == 'pull_request' && github.event.action == 'opened'
    steps:
      - name: Wait for approval
        id: approval
        uses: "peter-evans/wait-for-approval@v2.0.1"
        with:
          message: "Please review the changes and approve if everything looks good."
      - name: Step 3
        run: echo "Running Step 3"
